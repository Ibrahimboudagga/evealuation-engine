# Hosted pilot settings
APP_ENVIRONMENT=production
POSTGRES_PASSWORD=replace-with-a-long-random-password
DATABASE_URL=postgresql+psycopg://evaluation:replace-with-a-long-random-password@db:5432/evaluation
# Generate once and keep it unchanged:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
WORKSPACE_ENCRYPTION_KEY=
# Populate after completing the Setup Wizard, then restart the ui service.
WORKSPACE_API_TOKEN=
BOOTSTRAP_SECRET=replace-with-a-random-operator-setup-secret
